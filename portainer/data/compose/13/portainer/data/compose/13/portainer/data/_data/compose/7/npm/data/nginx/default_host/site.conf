# ------------------------------------------------------------
# Default Site
# ------------------------------------------------------------

server {
  listen 80 default;
#listen [::]:80;

  server_name default-host.localhost;
  access_log /data/logs/default_host.log combined;

  location / {
    return 404;
  }

}

